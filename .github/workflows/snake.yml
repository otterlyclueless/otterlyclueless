name: Generate Snake
on:
  schedule: [{cron: "0 */24 * * *"}]
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk/svg-only@v3
        with:
          github_user_name: otterlyclueless
          outputs: dist/snake.svg?color_snake=orange&color_dots=#0a0a0b,#ef4444,#f59e0b,#fca5a5,#ef4444
      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
